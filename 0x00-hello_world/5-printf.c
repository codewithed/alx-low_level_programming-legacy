#include <stdio.h>

/**
 * main - Execution of program starts here
 *
 * Return: Return 0 if successful, 1 if not successful
 */

int main(void)
{
	char string1[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s", string1);
	return (0);
}
